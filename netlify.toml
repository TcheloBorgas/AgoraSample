# Site estático em web/. Backend FastAPI noutro host.
# Netlify → Environment variables (Build):
#   SCHEDULER_API_BASE = https://teu-fastapi.exemplo.com  (sem / no fim)
[build]
  command = "node scripts/netlify-build-web.mjs"
  publish = "web"
