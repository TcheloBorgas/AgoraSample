/* Em deploy Netlify isto é regenerado vazio — o proxy /api usa SCHEDULER_API_BASE no build.
 * Desenvolvimento local (ex. abrir web/ sem proxy): descomente a linha seguinte. */
// window.__SCHEDULER_API_BASE__ = "http://127.0.0.1:8000";
window.__SCHEDULER_API_BASE__ = "";
